#include <stdio.h>
int main(){
	int a,counter = 0 ;
	printf("ENTER ANY NUMBER : ");
	scanf("%d",&a);
	for(int i = 1; i<=a;i++){
		if(a%i==0){
			counter++;
		}
	}
	if(counter > 2){
		printf("%d is not a prime number",a);
	}
	else {
		printf("%d is a prime number",a);
	}
}
